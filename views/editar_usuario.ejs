<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Usuario</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="../public/css/editar_productos.css">
    <link rel="icon" href="../public/img/ico.ico" type="image/x-icon">
</head>
<body>
    <header>
        <h2>Editar Usuario</h2>
    </header>
    
    <section class="container my-5">
        <form id="editarUsuarioForm" method="post" action="/usuarios/actualizar" class="card shadow p-4">
            <input type="hidden" name="id" value="<%= usuario.ID_Usuario %>">
            
            <!-- Campo Nombre -->
            <div class="mb-3">
                <label for="nombre" class="form-label">Nombre:</label>
                <input type="text" name="nombre" value="<%= usuario.Nombre_Usuario %>" class="form-control" required>
            </div>
            
            <!-- Campo Correo -->
            <div class="mb-3">
                <label for="correo" class="form-label">Correo:</label>
                <input type="email" name="correo" value="<%= usuario.correo_Usuario %>" class="form-control" required>
            </div>

            <!-- Botón de envío -->
            <div class="text-center">
                <button type="submit">Actualizar</button>
            </div>
        </form>
    </section>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
